---
description: Diagnose & fix a failing test (root cause, no masking)
---

Use the **test-healer** agent to run the suite, diagnose the failure by root cause, and
fix it without masking (no sleeps, skips, or loosened assertions).

Target: $ARGUMENTS

If no target is given, run the whole suite and heal whatever fails.
