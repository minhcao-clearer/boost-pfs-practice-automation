---
description: Debug and fix failing Playwright tests
---
Use the playwright-test-healer agent to debug and fix the failing Playwright test(s).

Target: $ARGUMENTS

If no target is given, ask which test file or test name to heal.
