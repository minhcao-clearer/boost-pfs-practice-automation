# Storefront base URL under test.
# Required: playwright.config.ts throws "BASE_URL is not set" if this is missing.
#
# For local runs, copy this file to .env:   cp .env.example .env
BASE_URL=https://boost-pfs-demo.myshopify.com
