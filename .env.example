# Copy this file to .env and fill in the value for local runs.
# In CI, set BASE_URL as an env var / repo variable instead.

# Base URL the tests navigate against (the Boost PFS demo storefront).
BASE_URL="https://boost-pfs-demo.myshopify.com/collections/vertical-layout"
